Use Jujutsu for repository history.

Keep Curriculum data out of this repository. Runtime skill Markdown, role data, manifests, request data, and generated consumer output belong to the external data repository or its consumer.

The CLI has one inline Datom configuration argument and no other configuration source.

Run and expose durable behavioral tests through Nix checks. Build only through configured remote builders.
